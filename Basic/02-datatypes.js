// Tipos de datos primitivos

// Cadenas de texto (string)

"Soy una cadena de texto y siempre voy entre comillas"

let nombre = "Eduardo Berrios";
let alias =  "Walicarpio";
let email = "eberrios@gmail.com";

console.log(nombre)
console.log("Cadena de texto")

// Números (number)

let edad = 56; // entero
let altura = 1.77; // decimal

// Booleanos (boolean)

let esProfesor = true;
let esEstudiante = false;

// Undefined

let undefindedValue // la variable ha sido declarada, pero no se le ha asignado o asociado ningún valor
console.log(undefindedValue) // por lo tanto el retorno no tiene nada que mostrar, osea es un valor undefined - no definido

// Null



// Symbol


// BigInt

// Mostramos los tipos de datos

console.log(typeof nombre)
console.log(typeof edad)
console.log(typeof esProfesor)
console.log(typeof undefindedValue)